baseURL: "https://adithyarn2005.github.io/"
canonifyURLs: true

languageCode: en-us
title: Adithya's Blog 

module:
  imports:
  - path: github.com/hugo-toha/toha/v4
  mounts:
  - source: static
    target: static
  - source: ./node_modules/flag-icon-css/flags
    target: static/flags
  - source: ./node_modules/@fontsource/mulish/files
    target: static/files
  - source: ./node_modules/katex/dist/fonts
    target: static/fonts

languages:
  en:
    languageName: English
    weight: 1


# Force a locale to be use, really useful to develop the application ! Should be commented in production, the "weight" should rocks.
# DefaultContentLanguage: bn

# Allow raw html in markdown file
markup:
  goldmark:
    renderer:
      unsafe: true
  tableOfContents:
    startLevel: 2
    endLevel: 6
    ordered: false

# At least HTML and JSON are required for the main HTML content and
# client-side JavaScript search
outputs:
  home:
    - HTML
    - RSS
    - JSON

# Enable global emoji support
enableEmoji: true

# Site parameters
params:
  background: images/bg_image.png

  site:
      verify:
        google: "e7rjdy1IrYoXeN8aq7jitEKWhbHxISSscTwEDLXFYpk"
    
  # GitHub repo URL of your site
  gitRepo: https://github.com/hugo-toha/hugo-toha.github.io


  features:
    # Enable portfolio section
    portfolio:
      enable: true

    darkMode:
      enable: true

    # Enable blog posts
    blog:
      enable: true

    # Enable Table of contents in reading page
    toc:
      enable: true

    analytics:
      enable: true
      services:
      # GoatCounter
      goatCounter:
        instance: https://adithyarn2005.goatcounter.com/count  # For self-hosted you should use only one of the two methods
  # Configure footer

  footer:
    enable: true
    template: footer.html
    disclaimer:
      enable: false
    copyright:
      enable: true
      text: ""

disableKinds:
  - sitemap
